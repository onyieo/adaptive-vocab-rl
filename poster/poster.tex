% CS224R Final Project Poster
% Stanford Gemini theme, 36"x24" landscape (recommended dimensions)
% Compile with XeLaTeX

\documentclass[final]{beamer}

% ====================
% Packages
% ====================

\usepackage[T1]{fontenc}
\usepackage{lmodern}
% 36" x 24" = 91.44 cm x 60.96 cm (rounded to 91 x 61 cm)
\usepackage[size=custom,width=91,height=61,scale=0.9]{beamerposter}
\usetheme{gemini}
\usecolortheme{stanford}

% Smaller title and section fonts (gemini defaults are too large at 36"x24")
\setbeamerfont{headline title}{family=\rmfamily, size=\LARGE, series=\bfseries}
\setbeamerfont{headline author}{family=\rmfamily, size=\large}
\setbeamerfont{headline institute}{family=\rmfamily, size=\normalsize}
\setbeamerfont{block title}{family=\rmfamily, size=\large, series=\bfseries}
\setbeamerfont{block alerted title}{family=\rmfamily, size=\large, series=\bfseries}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.17}

\graphicspath{{plots/}}

% ====================
% Lengths — 3 columns
% ====================

\newlength{\sepwidth}
\newlength{\colwidth}
\setlength{\sepwidth}{0.02\paperwidth}
\setlength{\colwidth}{0.31\paperwidth}

\newcommand{\separatorcolumn}{\begin{column}{\sepwidth}\end{column}}

% ====================
% Header / metadata
% ====================

\title{Offline RL for Adaptive Vocabulary Selection \\
       in Conversational Language Tutoring}

\author{Onyinyechi Okoye}

\institute[shortinst]{Computer Science, Stanford University \quad CS224R Spring 2026}

\footercontent{
  Code: \href{https://github.com/onyieo/adaptive-vocab-rl}{github.com/onyieo/adaptive-vocab-rl} \hfill
  CS224R Final Project \hfill
  \href{mailto:onyie@stanford.edu}{onyie@stanford.edu}}

% Suppress default logo placeholders (gemini reserves space if unset)
\logoright{}
\logoleft{}

\begin{document}

% Stanford logo top-right
\addtobeamertemplate{headline}{}{
    \begin{tikzpicture}[remember picture,overlay]
      \node [anchor=north east, inner sep=2cm]
            at ([xshift=0.0cm,yshift=1.5cm]current page.north east)
      {\includegraphics[height=5.0cm]{stanford_logos/Block_S_2_color.png}};
    \end{tikzpicture}
}

\begin{frame}[t]
\begin{columns}[t]
\separatorcolumn

% =====================================================================
% COLUMN 1 — Motivation (Q1+Q2), Prior Work (Q3), Approach (Q4)
% =====================================================================
\begin{column}{\colwidth}

  \begin{block}{Motivation}
    A conversational language tutor decides at every turn:
    \emph{which vocabulary should the response target?} The turn presents
    a mix of unseen words, words being acquired, and words due for review.
    The choice trades off:
    \begin{itemize}
      \item \textbf{Long-term retention} — words must return before they decay
      \item \textbf{Curriculum pacing} — too many new words overwhelms the learner
      \item \textbf{Conversational naturalness} — turns must sound like
            dialogue, not a flashcard drill
    \end{itemize}
    Existing products rely on hand-coded spaced-repetition rules. A
    principled reinforcement-learning approach has the potential to
    discover scheduling strategies that fixed heuristics cannot express.
  \end{block}

  \begin{block}{Related Work}
    \textbf{Spaced repetition (FSRS, SM-2)} \cite{fsrs} schedules
    isolated flashcards — no mechanism to embed review in dialogue, no
    joint reasoning about introducing new words.

    \textbf{RLHF-style methods} \cite{rlhf} treat the LLM as the policy and
    cannot reason about long-horizon retention.

    \textbf{Educational RL} \cite{rafferty} works on isolated problems,
    never on item selection mediated through a generative LLM.

    \textbf{Our framing}: a \emph{hierarchical control} architecture
    where RL operates over latent linguistic constraints and a frozen LLM
    realizes them as natural conversation.
  \end{block}

  \begin{block}{Approach}
    \begin{center}
      \begin{tikzpicture}[
          node distance=1.0cm and 0.7cm,
          box/.style={rectangle, draw=stanfordred, very thick, rounded corners,
                     minimum height=1.8cm, minimum width=7.0cm, align=center,
                     text width=6.4cm, font=\small},
          smallbox/.style={rectangle, draw=black!60, thick, rounded corners,
                     minimum height=1.8cm, minimum width=7.0cm, align=center,
                     text width=6.4cm, font=\small},
          arrow/.style={->, very thick, stanfordred, >=latex}]
        % Top row: policy -> action -> llm
        \node[box] (policy) {\textbf{RL Policy} $\pi(a|s)$\\(IQL / CQL)};
        \node[smallbox, right=of policy] (action) {\textbf{Directive}\\$(n_{\text{new}}, n_{\text{act}}, n_{\text{rev}})$};
        \node[smallbox, right=of action] (llm) {\textbf{Frozen LLM}\\(Claude Sonnet)};
        % Bottom row: state <- sim <- utterance
        \node[smallbox, below=of llm] (utter) {\textbf{Japanese}\\dialogue turn};
        \node[smallbox, below=of action] (sim) {\textbf{Student simulator}\\(FSRS dynamics)};
        \node[smallbox, below=of policy] (state) {\textbf{State $s$}\\(per-word stats)};
        \draw[arrow] (policy) -- (action);
        \draw[arrow] (action) -- (llm);
        \draw[arrow] (llm) -- (utter);
        \draw[arrow] (utter) -- (sim);
        \draw[arrow] (sim) -- (state);
        \draw[arrow] (state) -- (policy);
      \end{tikzpicture}
    \end{center}
    \vspace{0.2cm}
    \begin{itemize}
      \item \textbf{State}: per-word stability, $R$, difficulty, usage; session position. 500 JLPT N5--N3 words.
      \item \textbf{Action}: $(n_{\text{new}}, n_{\text{act}}, n_{\text{rev}})$ summing to 5 (21 discrete actions).
      \item \textbf{Methods}: IQL \cite{iql}, CQL \cite{cql} (offline, \emph{deployable});
            PPO \cite{ppo} (online, reference upper bound);
            heuristics: Random, FSRS, FSRS+New, RuleBased, SmartFSRS.
    \end{itemize}
  \end{block}

\end{column}

\separatorcolumn

% =====================================================================
% COLUMN 2 — Key finding (reward design) + headline retention
% =====================================================================
\begin{column}{\colwidth}

  \begin{alertblock}{Key Finding: Reward Design Dominates Algorithm Choice}
    Identical algorithms, two reward formulations, a 6$\times$ improvement
    in post-gap retention across every RL method.

    \heading{v3 reward}
    \vspace{-0.3cm}
    $$r_t = \underbrace{\Delta R_{\text{total}}/N}_{\text{coverage}}
            - \underbrace{\alpha \cdot \max(0, n_{\text{new}} {-} 1)^2}_{\text{naturalness penalty}}
            + \underbrace{\lambda \cdot \tfrac{\#\{R{>}0.5\}}{N}}_{\text{retention bonus}}$$
    \vspace{-0.2cm}
    \begin{figure}
      \centering
      \includegraphics[width=0.95\colwidth]{reward_design_impact.png}
    \end{figure}
  \end{alertblock}

  \begin{block}{Final Retention (500 words, 95\% CI)}
    \begin{figure}
      \centering
      \includegraphics[width=0.98\colwidth]{headline_retention_20seeds.png}
    \end{figure}
    \textbf{Offline RL (the deployable case) outperforms every fixed
    heuristic by 4--17$\times$} on post-gap retention. PPO is included as a
    \emph{non-deployable reference} (online RL requires student-in-the-loop
    training, which is impractical in production); it indicates an
    additional 2$\times$ headroom under unrestricted simulator access.
  \end{block}

\end{column}

\separatorcolumn

% =====================================================================
% COLUMN 3 — More findings + Discussion + Future Work
% =====================================================================
\begin{column}{\colwidth}

  \begin{block}{Adaptive Pacing Emerges}
    \begin{figure}
      \centering
      \includegraphics[width=0.98\colwidth]{action_distribution_v3.png}
    \end{figure}
    Action mix by turn-in-session. The learned policies (right panels)
    develop intra-session pacing: new words introduced early, review
    weighted later. No hand-coded baseline exhibits this structure.
  \end{block}

  \begin{block}{Retention vs Naturalness Pareto}
    \begin{figure}
      \centering
      \includegraphics[width=0.98\colwidth]{pareto_retention_naturalness.png}
    \end{figure}
    Naturalness scored by a cross-family LLM judge (OpenAI gpt-4o-mini
    evaluating a Claude Sonnet tutor) to mitigate same-family
    self-preference. Offline IQL achieves higher naturalness than PPO
    while maintaining $\sim$50\% of its retention.
  \end{block}

  \begin{block}{Discussion}
    \textbf{Takeaways:} reward design dominates algorithm choice for this
    domain; coverage metrics are misleading (Random scored 81 acquired but
    only 18 retained after 7 days); hierarchical RL+LLM control is a
    viable architecture for tutor-style applications.

    \textbf{Limitations:} simulator dynamics are hand-tuned without a
    human pilot; the action space is coarse (21 directives); offline RL
    confidence intervals remain wide enough to motivate further multi-seed
    work.
  \end{block}

  \begin{block}{Future Work}
    Top-$k$ per-word action space, behavior-cloning-of-PPO ablation, and
    extended out-of-distribution robustness across simulator
    parameterizations.
  \end{block}

  \begin{block}{References}
    \nocite{*}
    \footnotesize{\bibliographystyle{plain}\bibliography{poster}}
  \end{block}

\end{column}

\separatorcolumn
\end{columns}
\end{frame}

\end{document}
